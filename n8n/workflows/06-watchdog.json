{
  "name": "WF6 — Watchdog (Gerchik System)",
  "nodes": [
    {
      "parameters": {
        "rule": {
          "interval": [{ "field": "minutes", "minutesInterval": 10 }]
        }
      },
      "id": "cron-10m",
      "name": "Every 10 Minutes",
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.2,
      "position": [200, 400]
    },
    {
      "parameters": {
        "jsCode": "const cfg = $getWorkflowStaticData('global');\nif (!cfg._initialized) {\n  cfg.BYBIT_API_KEY = 'wS5MT4IqmNYxjLuBlP';\n  cfg.BYBIT_API_SECRET = 'ZmwhvqOx42QzLQ54tMzaUGScr9cSxLU8XklW';\n  cfg.BYBIT_TESTNET = 'true';\n  cfg.OPENROUTER_API_KEY = 'YOUR_OPENROUTER_API_KEY';\n  cfg.OPENROUTER_MODEL = 'anthropic/claude-sonnet-4-20250514';\n  cfg.TELEGRAM_CHAT_ID = '543663073';\n  cfg.TELEGRAM_BOT_TOKEN = 'YOUR_TELEGRAM_BOT_TOKEN';\n  cfg._initialized = true;\n}\nreturn [{ json: { status: 'ok' } }];"
      },
      "id": "config",
      "name": "Config",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [320, 400]
    },
    {
      "parameters": {
        "jsCode": "// =============================================\n// Watchdog: verify all workflows are running\n// and system is healthy\n// =============================================\n\nconst cfg = $getWorkflowStaticData('global');\n\nconst checks = [];\n\n// 1. Check Bybit API connectivity\ntry {\n  const isTestnet = (cfg.BYBIT_TESTNET || 'true').toLowerCase() === 'true';\n  const baseUrl = isTestnet ? 'https://api-testnet.bybit.com' : 'https://api.bybit.com';\n  const resp = await this.helpers.httpRequest({\n    method: 'GET',\n    url: `${baseUrl}/v5/market/time`\n  });\n  checks.push({\n    name: 'Bybit API',\n    status: resp.retCode === 0 ? 'OK' : 'ERROR',\n    detail: resp.retCode === 0 ? 'Connected' : resp.retMsg\n  });\n} catch(e) {\n  checks.push({ name: 'Bybit API', status: 'DOWN', detail: e.message });\n}\n\n// 2. Check OpenRouter API connectivity (Claude via OpenRouter)\ntry {\n  const resp = await this.helpers.httpRequest({\n    method: 'POST',\n    url: 'https://openrouter.ai/api/v1/chat/completions',\n    headers: {\n      'Authorization': `Bearer ${cfg.OPENROUTER_API_KEY}`,\n      'Content-Type': 'application/json'\n    },\n    body: {\n      model: cfg.OPENROUTER_MODEL || 'anthropic/claude-sonnet-4-20250514',\n      max_tokens: 10,\n      messages: [{ role: 'user', content: 'ping' }]\n    }\n  });\n  checks.push({\n    name: 'OpenRouter (Claude)',\n    status: resp.choices ? 'OK' : 'ERROR',\n    detail: 'Connected'\n  });\n} catch(e) {\n  checks.push({ name: 'OpenRouter (Claude)', status: 'DOWN', detail: e.message });\n}\n\n// 3. Check config keys\nconst requiredKeys = ['BYBIT_API_KEY', 'BYBIT_API_SECRET', 'OPENROUTER_API_KEY', 'TELEGRAM_CHAT_ID'];\nconst missingKeys = requiredKeys.filter(k => !cfg[k] || cfg[k].startsWith('YOUR_'));\nchecks.push({\n  name: 'Config',\n  status: missingKeys.length === 0 ? 'OK' : 'WARNING',\n  detail: missingKeys.length === 0 ? 'All set' : `Missing: ${missingKeys.join(', ')}`\n});\n\n// 4. Check bot stopped flag\nconst store = $getWorkflowStaticData('global');\nchecks.push({\n  name: 'Bot Status',\n  status: store.botStopped ? 'STOPPED' : 'RUNNING',\n  detail: store.botStopped ? `Stopped at ${new Date(store.stoppedAt).toLocaleString('ru-RU')}` : 'Active'\n});\n\n// Determine overall health\nconst hasErrors = checks.some(c => c.status === 'DOWN' || c.status === 'ERROR');\nconst hasWarnings = checks.some(c => c.status === 'WARNING' || c.status === 'STOPPED');\n\nreturn [{\n  json: {\n    checks,\n    healthy: !hasErrors,\n    hasWarnings,\n    needsAlert: hasErrors\n  }\n}];"
      },
      "id": "health-check",
      "name": "System Health Check",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [540, 400]
    },
    {
      "parameters": {
        "conditions": {
          "options": { "caseSensitive": true, "leftValue": "", "typeValidation": "strict" },
          "conditions": [{
            "leftValue": "={{ $json.needsAlert }}",
            "rightValue": true,
            "operator": { "type": "boolean", "operation": "true" }
          }],
          "combinator": "and"
        },
        "options": {}
      },
      "id": "if-unhealthy",
      "name": "System Unhealthy?",
      "type": "n8n-nodes-base.filter",
      "typeVersion": 2,
      "position": [780, 400]
    },
    {
      "parameters": {
        "jsCode": "const cfg = $getWorkflowStaticData('global');\n\nconst data = $input.first().json;\n\nlet msg = '⚠️ <b>Watchdog Alert</b>\\n\\n';\n\nfor (const check of data.checks) {\n  const emoji = {\n    'OK': '✅', 'ERROR': '❌', 'DOWN': '🔴',\n    'WARNING': '⚠️', 'STOPPED': '⛔', 'RUNNING': '🟢'\n  }[check.status] || '❓';\n  \n  msg += `${emoji} <b>${check.name}:</b> ${check.status}\\n`;\n  msg += `   ${check.detail}\\n\\n`;\n}\n\nif (!data.healthy) {\n  msg += '\\n🚨 <b>ВНИМАНИЕ: Система работает с ошибками!</b>\\n';\n  msg += 'Торговля приостановлена до устранения проблем.\\n';\n}\n\nmsg += `\\n⏱ ${new Date().toLocaleString('ru-RU', { timeZone: 'Europe/Moscow' })}`;\n\nreturn [{ json: { message: msg } }];"
      },
      "id": "format-alert",
      "name": "Format Watchdog Alert",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [1020, 400]
    },
    {
      "parameters": {
        "operation": "sendMessage",
        "chatId": "={{ $getWorkflowStaticData('global').TELEGRAM_CHAT_ID }}",
        "text": "={{ $json.message }}",
        "additionalFields": { "parse_mode": "HTML" }
      },
      "id": "send-alert",
      "name": "TG: Watchdog Alert",
      "type": "n8n-nodes-base.telegram",
      "typeVersion": 1.2,
      "position": [1260, 400],
      "credentials": {
        "telegramApi": { "id": "TELEGRAM_CREDENTIALS_ID", "name": "Telegram Bot" }
      }
    }
  ],
  "connections": {
    "Every 10 Minutes": {
      "main": [[ { "node": "Config", "type": "main", "index": 0 } ]]
    },
    "Config": {
      "main": [[ { "node": "System Health Check", "type": "main", "index": 0 } ]]
    },
    "System Health Check": {
      "main": [[ { "node": "System Unhealthy?", "type": "main", "index": 0 } ]]
    },
    "System Unhealthy?": {
      "main": [[ { "node": "Format Watchdog Alert", "type": "main", "index": 0 } ]]
    },
    "Format Watchdog Alert": {
      "main": [[ { "node": "TG: Watchdog Alert", "type": "main", "index": 0 } ]]
    }
  },
  "settings": { "executionOrder": "v1" },
  "tags": [{ "name": "gerchik-bot" }, { "name": "watchdog" }]
}
